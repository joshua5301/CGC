# Original Repo

https://github.com/XYGaoG/CGC/tree/main

# Q=I distance propagation (2-layer GCN)

See [the objective, bound qualifications and experiment protocol](docs/distance_identity.md).
`colab_distance.py` is a ready-to-paste Colab cell with Drive checkpointing and
resume support. `sweep_distance.py --preset pilot` compares the new method and
GRIP using the same 2-layer GCN and unchanged uniform student loss.

